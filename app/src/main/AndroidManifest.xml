<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/header"
        android:label="@string/app_name"
        android:roundIcon="@drawable/header"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApplicationCitra">

        <activity
            android:name=".CardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RelatifActivity"
            android:exported="false" />

        <activity
            android:name=".HasilForm"
            android:exported="false" />

        <activity
            android:name=".cafe"
            android:exported="false" />

        <activity
            android:name=".NotaActivity"
            android:exported="false" />

        <activity
            android:name=".kalku"
            android:exported="false" />

        <activity
            android:name=".KonversiSuhu"
            android:exported="false" />

        <activity
            android:name=".Profil"
            android:exported="false" />

    </application>
</manifest>